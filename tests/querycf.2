#!/bin/sh

harness="`dirname "$0"`/query-harness.sh"
wd="`pwd`"
td="`dirname "$0"`"

export HOME=/home/test
export XDG_CONFIG_DIRS="$wd/$td"

basename="`basename "$0"`"
arguments="config find $basename"
expected="$wd/$td/$basename"

. "$harness"
