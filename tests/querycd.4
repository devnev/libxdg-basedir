#!/bin/sh

harness="`dirname "$0"`/query-harness.sh"

export HOME=/home/test
export XDG_CONFIG_DIRS=/etc/config:/etc/xdg:/usr/share/config:.

arguments='config dirs'
expected="\
/etc/config
/etc/xdg
/usr/share/config
."

. "$harness"
